use std::env;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() > 2 && args[1] == "add" {
        println!("Добавление новой задачи: {}", args[2]);
    } else {
        println!("ToDo CLI v0.1. Используйте 'add <текст>'");
    }
}
